{
  "name": "viewport-screenshot",
  "type": "module",
  "version": "1.0.0",
  "author": "NazCodeland",
  "main": "plguin.ts",
  "scripts": {
    "build": "bun build ./src/plugin.ts --minify --outdir ./dist",
    "watch": "bun run build --watch",
    "test": "bun run test",
    "format": "bunx biome format",
    "lint": "bunx biome lint",
    "check": "bunx biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.1",
    "@figma/plugin-typings": "^1.100.0"
  },
  "description": "Your Figma Plugin",
  "license": "",
  "dependencies": {
    "bun": "^1.1.22"
  }
}