{
  "name": "viewport-screenshot",
  "type": "module",
  "version": "1.0.0",
  "author": "NazCodeland",
  "main": "code.js",
  "scripts": {
    "build": "bun build ./src/plugin.ts --outdir ./src/  --minify --sourcemap=inline",
    "watch": "bun run build --watch",
    "dev": "bun run --watch ./src/plugin ",
    "test": "bun run test",
    "format": "bunx biome format",
    "lint": "bunx biome lint",
    "check": "bunx biome check --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.0",
    "@figma/plugin-typings": "^1.99.0",
    "typescript": "^5.5.4"
  },
  "description": "Your Figma Plugin",
  "license": "",
  "dependencies": {
    "bun": "^1.1.22"
  }
}